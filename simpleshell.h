#ifndef DEF_SIMPLESHELL
#define DEF_SIMPLESHELL

#include <unistd.h>
#include <stdio.h>
#include <stdlib.h>
#include <sys/types.h>
#include <sys/wait.h>
#include <string.h>

int _putchar(char c);
int _putstr(char *s);
int _strcmp(char *s1, char *s2);
int _strlen(char *s);

#endif /*DEF_SIMPLESHELL*/
